import VickyCollapseItem from './item.vue'

/* istanbul ignore next */
VickyCollapseItem.install = function (Vue) {
  Vue.component(VickyCollapseItem.name, VickyCollapseItem)
}

export default VickyCollapseItem
